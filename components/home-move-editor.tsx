"use client";
import { FormGrid } from '@/components/ui/planner-layout';
import { Amount } from '@/components/budget-editor';
import { Input } from '@/components/ui/input';
import { EMPTY_HOME_MOVE, type HomeMove } from '@/lib/home-move';
import type { PlannerData } from '@/lib/planner';
export function HomeMoveEditor({plan,move,onChange}:{plan:PlannerData;move?:HomeMove;onChange:(move:HomeMove|undefined)=>void}){
 const update=(patch:Partial<HomeMove>)=>onChange({...move!,...patch});
 return <details><summary>Home Sale & Cash-Funded Downsize</summary><p>Sell the entered home, settle its mortgage, then rent or buy replacement housing with cash. Proceeds are an asset transfer, not wages. A new mortgage, multiple liens, rental depreciation, nonqualified use, partial exclusions and installment sales are outside this supported path.</p><label className="budget-check"><input type="checkbox" checked={!!move} onChange={e=>onChange(e.target.checked?structuredClone(EMPTY_HOME_MOVE):undefined)}/> Include a Home Move</label>{move&&<><FormGrid as="div" className="budget-fields"><label className="budget-field"><span>Sale Month</span><Input aria-label="Sale Month" type="month" value={move.month} onChange={e=>update({month:e.target.value})}/></label><label className="budget-field"><span>Mortgage Settled at Sale</span><select aria-label="Mortgage Settled at Sale" value={move.mortgageId} onChange={e=>update({mortgageId:e.target.value})}><option value="">No Mortgage</option>{plan.debts.filter(d=>d.kind==='mortgage').map(d=><option key={d.id} value={d.id}>{d.name||'Mortgage'}</option>)}</select></label>{(['salePrice','sellingCosts','adjustedBasis','replacementPrice','replacementClosingCosts','newMonthlyHousing','newAnnualPropertyTax','newAnnualInsurance'] as const).map(key=><Amount key={key} label={{salePrice:'Home Sale Price',sellingCosts:'Selling Costs',adjustedBasis:'Adjusted Home Tax Basis',replacementPrice:'Replacement Home Cash Price',replacementClosingCosts:'Replacement Closing Costs',newMonthlyHousing:'New Rent / HOA / Maintenance per Month',newAnnualPropertyTax:'New Property Tax per Year',newAnnualInsurance:'New Home / Renters Insurance per Year'}[key]} value={move[key]} onChange={v=>update({[key]:v})}/>)}<label className="budget-field"><span>Home Gain Exclusion</span><select aria-label="Home Gain Exclusion" value={move.exclusion} onChange={e=>update({exclusion:e.target.value as HomeMove['exclusion']})}><option value="none">No Exclusion</option><option value="standard">Confirmed Standard Full Exclusion</option></select></label></FormGrid><p className="field-help">Use zero for replacement purchase costs when renting. New recurring housing replaces the old property-tax/insurance amounts from the sale month; remove any overlapping everyday housing rows yourself. Mortgage payoff is calculated from the debt schedule, not subtracted from tax basis. Lender fees/daily payoff interest are excluded. Personal-home losses do not create a deductible capital loss.</p><label className="budget-check"><input type="checkbox" checked={move.standardCaseConfirmed} onChange={e=>update({standardCaseConfirmed:e.target.checked})}/> I confirm a long-term, standard personal-home sale with none of the excluded special cases.</label>{move.exclusion==='standard'&&<label className="budget-check"><input type="checkbox" checked={move.eligibilityConfirmed} onChange={e=>update({eligibilityConfirmed:e.target.checked})}/> I confirm the ownership, residence, look-back and filing-status eligibility for the full exclusion.</label>}<p className="field-help">The standard cap is $250,000, or $500,000 for qualifying married joint filers. For the joint cap, both spouses must meet residence/look-back requirements and at least one must meet ownership requirements. Review <a href="https://www.irs.gov/publications/p523" target="_blank" rel="noreferrer">IRS Publication 523 (2025), Eligibility Test and Worksheets 1–3</a>. Source verified September 7, 2026. Tax on nonexcluded gain feeds the shared capital-gains/NIIT calculation; state tax remains an estimate.</p></>}</details>;
}
